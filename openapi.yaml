# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: CallHello API
    description: CallHello 定义一个简单的 Hello 服务
    version: 0.0.1
paths:
    /CallHello/Hello:
        post:
            tags:
                - CallHello
            operationId: CallHello_Hello
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/krathub.hello.v1.HelloRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/krathub.hello.v1.HelloResponse'
components:
    schemas:
        krathub.hello.v1.HelloRequest:
            type: object
            properties:
                req:
                    type: string
            description: HelloRequest 是 Hello 服务的请求消息
        krathub.hello.v1.HelloResponse:
            type: object
            properties:
                rep:
                    type: string
            description: HelloResponse 是 Hello 服务的响应消息
tags:
    - name: CallHello
