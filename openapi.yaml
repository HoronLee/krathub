# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Auth API
    version: 0.0.1
paths:
    /v1/auth/login/using-password:
        post:
            tags:
                - Auth
            operationId: Auth_LoginByPassword
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/auth.v1.LoginByPasswordRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/auth.v1.LoginByPasswordReply'
    /v1/auth/signup/using-email:
        post:
            tags:
                - Auth
            operationId: Auth_SignupByEmail
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/auth.v1.SignupByEmailRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/auth.v1.SignupByEmailReply'
components:
    schemas:
        auth.v1.LoginByPasswordReply:
            type: object
            properties:
                token:
                    type: string
            description: 密码登录响应
        auth.v1.LoginByPasswordRequest:
            type: object
            properties:
                loginId:
                    type: string
                password:
                    type: string
                captchaId:
                    type: string
                captchaAnswer:
                    type: string
            description: 密码登录请求
        auth.v1.SignupByEmailReply:
            type: object
            properties:
                data:
                    $ref: '#/components/schemas/auth.v1.UserInfo'
                token:
                    type: string
            description: 邮箱注册响应
        auth.v1.SignupByEmailRequest:
            type: object
            properties:
                name:
                    type: string
                password:
                    type: string
                    description: 密码最小长度5，最大长度10
                passwordConfirm:
                    type: string
                verifyCode:
                    type: string
                email:
                    type: string
                phone:
                    type: string
            description: 邮箱注册请求
        auth.v1.UserInfo:
            type: object
            properties:
                id:
                    type: string
                name:
                    type: string
                email:
                    type: string
                phone:
                    type: string
            description: 用户信息
tags:
    - name: Auth
